name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    name: 🔨 Compilation et Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Récupération du code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Nécessaire pour SonarQube
      
      - name: 🛠️ Configuration .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'
      
      - name: 🧰 Restauration des dépendances
        run: dotnet restore
      
      - name: 🏗️ Compilation du projet
        run: dotnet build --no-restore --configuration Release
      
      - name: 🧪 Exécution des tests unitaires
        run: dotnet test --no-build --configuration Release --verbosity normal /p:CollectCoverage=true /p:CoverletOutputFormat=opencover /p:CoverletOutput=./coverage.opencover.xml
      
      - name: 📊 Publication des résultats de tests
        uses: actions/upload-artifact@v3
        with:
          name: test-results
          path: |
            **/coverage.opencover.xml
            **/*.trx
  
  trivy-scan:
    name: 🔍 Analyse des dépendances
    runs-on: ubuntu-latest
    needs: build-and-test
    
    steps:
      - name: 📥 Récupération du code
        uses: actions/checkout@v3
      
      - name: 🛠️ Installation de Trivy
        run: |
          sudo apt-get install wget apt-transport-https gnupg lsb-release -y
          wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | sudo apt-key add -
          echo deb https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main | sudo tee -a /etc/apt/sources.list.d/trivy.list
          sudo apt-get update
          sudo apt-get install trivy -y
      
      - name: 🔍 Analyse des dépendances avec Trivy
        run: |
          mkdir -p rapports
          trivy fs --format json --output rapports/trivy-deps.json .
          trivy fs --severity CRITICAL,HIGH --format table .
      
      - name: 📊 Publication des rapports Trivy
        uses: actions/upload-artifact@v3
        with:
          name: trivy-report
          path: rapports/trivy-deps.json
  
  trivy-docker-scan:
    name: 🔍 Analyse Docker avec Trivy
    runs-on: ubuntu-latest
    needs: build-and-test
    
    steps:
      - name: 📥 Récupération du code
        uses: actions/checkout@v3
      
      - name: 🔍 Analyse Docker avec Trivy
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'config'
          format: 'json'
          output: 'trivy-dockerfile.json'
          severity: 'CRITICAL,HIGH'
        
      - name: 📊 Publication des rapports Trivy Docker
        uses: actions/upload-artifact@v3
        with:
          name: trivy-docker-report
          path: trivy-dockerfile.json
  
  sonarqube-analysis:
    name: 📈 Analyse de qualité du code
    runs-on: ubuntu-latest
    needs: build-and-test
    
    steps:
      - name: 📥 Récupération du code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: 🛠️ Configuration .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'
      
      - name: 🧰 Installation de SonarScanner
        run: dotnet tool install --global dotnet-sonarscanner
      
      - name: 📥 Téléchargement des résultats de tests
        uses: actions/download-artifact@v3
        with:
          name: test-results
      
      - name: 📊 Analyse SonarQube
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        run: |
          dotnet sonarscanner begin \
            /k:"ECF_2_DOTNET" \
            /n:"ECF_2_DOTNET" \
            /d:sonar.host.url="${SONAR_HOST_URL}" \
            /d:sonar.login="${SONAR_TOKEN}" \
            /d:sonar.cs.opencover.reportsPaths="**/coverage.opencover.xml" \
            /d:sonar.cs.vstest.reportsPaths="**/*.trx" \
            /d:sonar.coverage.exclusions="**Test*.cs" \
            /d:sonar.sourceEncoding="UTF-8"
          
          dotnet build --no-incremental
          
          dotnet sonarscanner end /d:sonar.login="${SONAR_TOKEN}"
      
      - name: 📝 Création du rapport d'information SonarQube
        run: |
          mkdir -p rapports
          echo "Analyse SonarQube" > rapports/sonar-analysis-info.txt
          echo "=================" >> rapports/sonar-analysis-info.txt
          echo "Date: $(date '+%d/%m/%Y %H:%M:%S')" >> rapports/sonar-analysis-info.txt
          echo "Projet: ECF_2_DOTNET" >> rapports/sonar-analysis-info.txt
          echo "URL des résultats: ${SONAR_HOST_URL}/dashboard?id=ECF_2_DOTNET" >> rapports/sonar-analysis-info.txt
      
      - name: 📊 Publication du rapport SonarQube
        uses: actions/upload-artifact@v3
        with:
          name: sonar-report
          path: rapports/sonar-analysis-info.txt
  
  e2e-tests:
    name: 🌐 Tests End-to-End
    runs-on: ubuntu-latest
    needs: build-and-test
    
    steps:
      - name: 📥 Récupération du code
        uses: actions/checkout@v3
      
      - name: 🛠️ Configuration .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'
      
      - name: 🧰 Restauration des dépendances
        run: dotnet restore
      
      - name: 🏗️ Compilation du projet
        run: dotnet build --no-restore --configuration Release
      
      - name: 🎭 Installation des navigateurs Playwright
        run: npx playwright install chromium
      
      - name: 🚀 Démarrage de l'application
        run: |
          dotnet run --project EcfDotnet.csproj --urls=http://localhost:5000 &
          sleep 10
      
      - name: 🧪 Exécution des tests E2E
        run: dotnet test EcfDotnet.E2ETests.New/EcfDotnet.E2ETests.New.csproj --no-build --configuration Release
      
      - name: 📊 Publication des rapports Playwright
        uses: actions/upload-artifact@v3
        with:
          name: playwright-report
          path: |
            playwright-report.html
            *.png
  
  publish-reports:
    name: 📋 Publication des rapports
    runs-on: ubuntu-latest
    needs: [build-and-test, e2e-tests, trivy-scan, trivy-docker-scan, sonarqube-analysis]
    if: always()
    
    steps:
      - name: 📥 Récupération du code
        uses: actions/checkout@v3
      
      - name: 📥 Téléchargement de tous les rapports
        uses: actions/download-artifact@v3
        with:
          path: all-reports
      
      - name: 📦 Préparation des rapports
        run: |
          mkdir -p rapports-consolides
          cp -r all-reports/test-results/* rapports-consolides/ || true
          cp -r all-reports/playwright-report/* rapports-consolides/ || true
          cp -r all-reports/trivy-report/* rapports-consolides/ || true
          cp -r all-reports/trivy-docker-report/* rapports-consolides/ || true
          cp -r all-reports/sonar-report/* rapports-consolides/ || true
      
      - name: 📊 Création du rapport de synthèse
        run: |
          cat > rapports-consolides/rapport-synthese.html << EOL
          <!DOCTYPE html>
          <html lang="fr">
          <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0">
              <title>Rapport de Synthèse - ECF_2_DOTNET</title>
              <style>
                  body { font-family: Arial, sans-serif; line-height: 1.6; margin: 0; padding: 20px; color: #333; }
                  h1 { color: #2c3e50; border-bottom: 2px solid #eee; padding-bottom: 10px; }
                  h2 { color: #3498db; margin-top: 20px; }
                  .section { background-color: #f8f9fa; padding: 15px; border-radius: 5px; margin-bottom: 20px; }
                  .success { color: #28a745; }
                  .warning { color: #ffc107; }
                  .danger { color: #dc3545; }
                  .info { color: #17a2b8; }
              </style>
          </head>
          <body>
              <h1>Rapport de Synthèse - ECF_2_DOTNET</h1>
              <div class="section">
                  <h2>Informations Générales</h2>
                  <p>Date du rapport: $(date '+%d/%m/%Y %H:%M:%S')</p>
                  <p>Branche: \${{ github.ref_name }}</p>
                  <p>Commit: \${{ github.sha }}</p>
              </div>
              
              <div class="section">
                  <h2>Résultats des Tests</h2>
                  <p>Les résultats détaillés des tests sont disponibles dans les artefacts du pipeline.</p>
              </div>
              
              <div class="section">
                  <h2>Analyse des Dépendances (Trivy)</h2>
                  <p>Le rapport complet est disponible dans les artefacts du pipeline.</p>
              </div>
              
              <div class="section">
                  <h2>Analyse Docker avec Trivy</h2>
                  <p>Le rapport complet est disponible dans les artefacts du pipeline.</p>
              </div>
              
              <div class="section">
                  <h2>Analyse de Qualité du Code (SonarQube)</h2>
                  <p>Le rapport complet est disponible sur SonarQube.</p>
              </div>
          </body>
          </html>
          EOL
      
      - name: 📊 Publication des rapports consolidés
        uses: actions/upload-artifact@v3
        with:
          name: rapports-consolides
          path: rapports-consolides/
